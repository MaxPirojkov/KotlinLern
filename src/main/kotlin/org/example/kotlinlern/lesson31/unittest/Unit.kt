package org.example.kotlinlern.lesson31.unittest
// Юнит тестирование, проверяют код на низком уровне, проверяют функцию, что работает правильно

class Calculator() {
    fun plus(a: Int, b: Int) = a + b
}
